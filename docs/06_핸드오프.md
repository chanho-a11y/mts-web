# 핸드오프 문서 (Handoff) — MTSPACE × NORMCORE 자사몰

> 문서 버전: v1.0 · 최종 갱신: 2026-06-05
> 목적: 새 세션·개발자가 **이 문서만 읽고 작업을 이어받을 수 있도록** 인프라·환경·규칙·현재 상태·다음 액션을 한곳에 모은다. 상세 설계는 01~05 참조.

---

## 1. 한 줄 요약

MTSPACE COFFEE(B2B, mtspace.coffee) × NORMCORE COFFEE(B2C, normcorecoffee.com)를 **단일 Next.js+Supabase 백엔드**가 2도메인으로 운영. 제품 등록 1회로 상세·라벨·카드뉴스·블로그·인스타 자산 자동생성. 회원 등급별 차등가격, 멀티 PG(이니시스·카카오페이·페이팔), CS 동적 FAQ, AIEO/SEO·성능 최적화.

## 2. 문서 지도

| 파일 | 역할 |
|---|---|
| 01_자사몰_구축_지침.md | 비즈니스 요구·원칙·결정(P0/P1/P2) — 충돌 시 최우선 |
| 02_skills.md | 작업 규칙(불변 규칙·플레이북) |
| 03_기술사양_명세서.md | 아키텍처·DB·API·결제·SEO·자산생성·FAQ |
| 04_신규웹사이트_기획보고서.md | 현행 전수분석 + 신규 IA·기능·매핑 |
| 05_작업계획_및_실행로드맵.md | 풀스코프 빌드 Phase·순서·블로커 |
| 06_핸드오프.md (본 문서) | 인프라·env·현재상태·다음 액션 |

## 3. 인프라 (실측 IDs)

| 자원 | 식별자 |
|---|---|
| Supabase 프로젝트 | `mtspace-commerce` · ref `apiskyivlvebpvvxfejq` · region `ap-northeast-2`(서울) · org `wqzciqchpmdbaqagmjjw` |
| Supabase DB 호스트 | `db.apiskyivlvebpvvxfejq.supabase.co` (생성 직후 초기화 중 — get_project로 상태 확인) |
| Vercel 팀 | `chanho-5430's projects` · `team_IVzsYqLiFp67O41wFRtWzsx0` |
| GitHub | **미연결** — 인증/생성 필요(§7) |
| 현 운영 | Shopify(mtspace.coffee), GoDaddy(normcorecoffee.com), 네이버 스마트스토어(normcorecoffee) |

## 4. 기술 스택·규칙 (요약)

- Next.js 14 App Router · TypeScript(strict) · Tailwind + shadcn/ui · Supabase(Postgres·Auth·Storage·RLS) · Vercel.
- 디자인 토큰: **Helvetica Neue · 자간 -2pt · 헤드라인 bold · 본문 light · 줄간격 160**. 브랜드 워드마크 불가침.
- 결제: provider 어댑터(`inicis|kakaopay|paypal`), 서버 승인·금액/통화 검증·멱등성.
- 불변 규칙(02): 데이터 단일소스, SKU 필수, B2C/B2B·브랜드 격리(RLS), 가격 서버 단일결정(개별가→등급가→정가, +기업 고객별 할인), 제품=자산 단일소스, 텍스트 네이티브/이미지 래스터, 결제정보 비저장, 검수 게이트.

## 5. 환경 변수 (예정 — Vercel/서버 전용)

```
NEXT_PUBLIC_SUPABASE_URL / SUPABASE_SERVICE_ROLE_KEY / NEXT_PUBLIC_SUPABASE_ANON_KEY
INICIS_MID / INICIS_SIGNKEY            # KG이니시스
KAKAOPAY_CID / KAKAOPAY_SECRET         # 카카오페이
PAYPAL_CLIENT_ID / PAYPAL_SECRET       # 페이팔(해외)
DAUM_POSTCODE (클라이언트 스크립트)      # 한국 주소검색
GMAIL_* (OAuth)                        # 이메일 발송/자동화
META_COMMERCE_* / GA4_ID / META_PIXEL  # 마케팅·분석
```
(실제 키는 사용자 제공 후 Vercel 환경변수에 저장. DB 미저장.)

## 6. 현재 상태 (2026-06-15 갱신)

- [x] 현행 Shopify 전수분석(04), 지침/스킬/기술사양(01~03) v1.4까지 완료.
- [x] 풀스코프 결정 확정(05 §0), 작업계획(05)·핸드오프(06) 작성.
- [x] **Supabase 프로젝트** `apiskyivlvebpvvxfejq`(서울) ACTIVE_HEALTHY.
- [x] **마이그레이션 적용**: 01 기반(브랜드·회원5등급·보안질문·사업자·가격티어) · 02 카탈로그(상품·SKU·등급가·개별가·resolve_price·재고) · 03 커머스(장바구니·주문·결제 provider·배송·쿠폰·팁) · 04 **EMS 프리미엄 요금 시드(14개국×63구간=882행)**.
- [x] **GitHub 레포 푸시 완료**: https://github.com/chanho-a11y/mts-web (P0 스캐폴드).
- [x] **P0 스캐폴드**: Next.js14·2도메인 미들웨어·i18n·Supabase·디자인토큰. 빌드 검증 통과.
- [x] **기획 문서 → `new-website/docs/`** 이전(01~06 + reference/EMS_premium_rates_epost.pdf).
- [x] **Vercel 프로젝트 연결·자동배포·검증 완료**: `mts-web`(team `chanho-5430s-projects`), 배포 URL 정상(200), x-brand/x-locale 라우팅 OK, Supabase `DB: ok`(카테고리 8종 로딩). GitHub→Vercel→Supabase 삼각연동 검증.
- [ ] P1 제품 시드(현 Shopify 27종 → 신규 DB, key_color 포함).
- [ ] 마케팅·콘텐츠 스키마(05 마이그레이션) + P2~P8 기능 빌드.
- [ ] 01~04 문서 v1.5 갱신(2도메인 역할매핑·풀스코프·EMS·팁·보안질문·i18n·회원등급).

> EMS 주의: weight_g 사다리는 0.5kg 균일 가정. 국제배송=커피 원두만이라 저중량 구간 위주로 신뢰. 고중량 구간은 우체국 원표와 대조 검증 권장.

## 7. 확정 사항 (2026-06-05 추가)

- **GitHub 레포**: https://github.com/chanho-a11y/mts-web (생성됨). ⚠️ 자동 푸시는 인증 필요 — GitHub 플러그인이 동적 등록 미지원이라 **사용자가 `/mcp`로 수동 인증** 또는 **PAT 제공** 필요. 그 전까지 레포 푸시·Vercel-GitHub 연동 보류.
- **EMS 요금표 출처**: https://ems.epost.go.kr/front.EmsDeliveryDelivery02.postal (우체국 EMS 요금 조회). → 국가별 무게/부피 요금 디지털화 시드 소스.
- **결제 키**: **구조만 준비**(provider 어댑터·env 자리)하고 키는 추후 추가.
- **팁**: 프리셋 **5% / 10% / 15% / 20% + 사용자지정%**. (B2C·일반회원 체크아웃, 기업 제외)
- **보안질문**: **아이디/비밀번호 찾기 용도**. 답변 해시 저장.
- **페이팔**: 통화 **USD**. 국제배송 **커피 원두만**(머천다이즈·기타 제외).

## 7-1. 남은 블로커

1. **GitHub 푸시 권한**(위) — 레포 스캐폴드 푸시·Vercel 연동의 전제.
2. **결제 운영 키**(추후) — 구조는 먼저 구현.
3. Gmail/Meta 연동 계정 권한(P6 시점).

## 8. 다음 액션 (순서)

1. GitHub 해소 → 레포 생성·Vercel 연결.
2. P0 스캐폴드(2도메인 라우팅·i18n·디자인토큰·Auth).
3. P1 스키마 마이그레이션(`apiskyivlvebpvvxfejq`)·RLS·`resolve_price`·시드(상품·key_color·EMS).
4. P2~P8 순차(05 로드맵).

## 8-1. 빌드 진행 로그 (구현 완료분)

- **P0 기반**: Next.js14·2도메인 Host 라우팅·i18n(ko/en geo)·디자인토큰·Supabase·Vercel 자동배포. ✅ 배포 검증.
- **P1 제품 시드**: 블렌드 15 + 아리차 1 = 16종(SKU·key_color·브랜드/스토어프론트·카테고리). ✅
- **P2 스토어프론트**: 헤더(소셜·언어토글·버거·로그인)·홈(히어로·카테고리·베스트·카테고리섹션)·카테고리·상세(지정순서+key_color테마+JSON-LD)·푸터. ✅
- **P3 회원/인증**: 가입(일반/기업·주소검색·보안질문해시·자동로그인)·로그인·마이페이지·DB트리거. ✅
- **P3b 커머스**: 장바구니(localStorage)·팁(5/10/15/20/커스텀, B2C)·체크아웃·**결제 provider 어댑터(이니시스/카카오페이/페이팔, 키 추후)**·주문/결제 생성(가격 서버 재계산 resolve_price). ✅ (로그인 사용자 기준; 게스트 체크아웃은 service_role 추후)
- **P4 관리자(부분)**: 역할 게이트·대시보드·주문/제품 목록·**사업자 승인(→ 도매 등급 부여)**. ✅
- **P7 동적 FAQ(부분)**: faq 테이블·FAQPage JSON-LD·카테고리 그룹·시드 8종. ✅
- **정보 페이지**: About·Contact(폼→contact_message)·Coffeelog·정책 5종. ✅

### 남은 작업 (다음 단계)
- P5 디자인 스튜디오 임포트(제품등록=상세·라벨·카드뉴스·블로그·인스타 자동생성).
- P6 마케팅(프로모션·인플루언서·Gmail 이메일·자동화·메타쇼핑·분석).
- 결제 운영키 연동(이니시스/카카오페이/페이팔) + 게스트 체크아웃(service_role).
- 배송비 계산 와이어링(국내 무게구간 + EMS), 세금계산서, 구독.
- 이미지: Archive.zip 원본 → WebP 변환·CMS 이미지 슬롯 매핑.
- 관리자 접근: 가입 후 해당 프로필 `role='admin'` 승격 필요(아래).

> **관리자 만들기**: chanho@mtspace.coffee로 가입 후, Supabase에서 `update profiles set role='admin' where email='chanho@mtspace.coffee';` 실행(또는 요청 시 처리).

## 9. 결정 로그 (변경 시 추가)

- 2026-06-05: 2도메인 분리·단일백엔드 / 풀스코프 / 가입시 일반·기업 선택 / 인프라 자동생성 / 결제 3 PG / Supabase 서울. (사용자 승인)
- 2026-06-15: GitHub 레포 푸시·Vercel 자동배포 연동 완료. P0~P4(부분)·P7(부분) 구현. 매 단계 푸시 대신 일괄 푸시로 전환(사용자 요청).
